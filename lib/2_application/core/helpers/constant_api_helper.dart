abstract class ConstantApiHelper {
  static const mainUrl = 'https://api.data.gov.sg/v1';
  static const trafficImages = '/transport/traffic-images';

  static const trafficImagesUrl = mainUrl + trafficImages;
}